## What this changes

<!-- The behavior before and after, from the point of view of someone using
     the driver. Link the issue if there is one. -->

## Checklist

- [ ] `./gradlew check` passes (unit tests, artifact checks, formatting)
- [ ] `./gradlew integrationTest` passes against a CrateDB the change targets
- [ ] `CHANGES.txt` has an entry under `Unreleased`, if the change is visible
      to someone using the driver
- [ ] The documentation under `docs/` matches the new behavior
